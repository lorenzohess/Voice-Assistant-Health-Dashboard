# Health Dashboard - Python Dependencies
# Compatible with Python 3.9+ on both Arch Linux and Raspberry Pi OS

# Web framework
Flask>=3.0.0
Werkzeug>=3.0.1

# Database ORM
Flask-SQLAlchemy>=3.1.1
SQLAlchemy>=2.0.36

# Charts (Plotly for interactive graphs)
plotly==5.18.0

# Scheduling (for alarms and timed events)
APScheduler==3.10.4

# Fuzzy matching (for food search in Phase 2)
rapidfuzz==3.5.2

# Date/time utilities
python-dateutil==2.8.2

# HTTP client (for LLM API calls)
requests>=2.31.0

# For CSV export
# (csv is built-in, no extra package needed)

# Speech-to-Text options
vosk==0.3.45  # Fast, less accurate
faster-whisper>=1.0.0  # Slower, more accurate (optional)
# Moonshine: pip install useful-moonshine-onnx@git+https://github.com/usefulsensors/moonshine.git#subdirectory=moonshine-onnx

# Audio processing
pyaudio==0.2.14
openwakeword==0.6.0
piper-tts==1.2.0
sounddevice==0.4.6
numpy==1.26.4

# Hardware controls (Raspberry Pi)
gpiozero>=2.0
adafruit-circuitpython-ads1x15>=2.2.0
